plugins {
    id 'com.android.application'
}

android {
    namespace 'com.ilia.photomasterai'
    compileSdk 35

    defaultConfig {
        applicationId 'com.ilia.photomasterai'
        minSdk 26
        targetSdk 35
        versionCode 5
        versionName '0.13.0-beta'

        def defaultEndpoint = 'https://photo-master-ai-backend-smilemangaphotography-2924.vercel.app/api/edit'
        def endpoint = project.findProperty('PHOTO_MASTER_AI_ENDPOINT') ?: defaultEndpoint
        buildConfigField 'String', 'AI_ENDPOINT', '"' + endpoint.toString().replace('\\', '\\\\').replace('"', '\\"') + '"'
    }

    buildFeatures { buildConfig true }

    buildTypes {
        debug { minifyEnabled false }
        release { minifyEnabled false }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies { }
