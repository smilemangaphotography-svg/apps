name: Legacy KINETIQ 3.0.7 Builder (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy builder retired
        run: |
          echo "The automatic 3.0.7 builder is retired."
          echo "Use build-kinetiq-update-signed.yml for the canonical signed KINETIQ update."
