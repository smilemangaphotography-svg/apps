name: Build FRAME Redesign Beta

on:
  workflow_dispatch:
  push:
    branches: [ frame-redesign-main ]
    paths:
      - 'FRAME-Redesign/beta/**'
      - '.github/workflows/build-frame-redesign-beta.yml'

permissions:
  contents: read

jobs:
  build-frame-beta:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout redesign branch
        uses: actions/checkout@v4

      - name: Validate locked UI assets
        shell: bash
        run: |
          test -f FRAME-Redesign/beta/app/src/main/assets/index.html
          test -f FRAME-Redesign/beta/app/src/main/assets/app.css
          test -f FRAME-Redesign/beta/app/src/main/assets/app.js
          test -f FRAME-Redesign/beta/app/src/main/assets/beta04.js
          test -f FRAME-Redesign/beta/app/src/main/java/com/ilia/frame/redesignbeta/SmartMlBridge.java
          node --check FRAME-Redesign/beta/app/src/main/assets/app.js
          node --check FRAME-Redesign/beta/app/src/main/assets/beta04.js
          grep -q 'app.css' FRAME-Redesign/beta/app/src/main/assets/index.html
          grep -q 'app.js' FRAME-Redesign/beta/app/src/main/assets/index.html
          grep -q 'beta04.js' FRAME-Redesign/beta/app/src/main/java/com/ilia/frame/redesignbeta/MainActivity.java
          grep -q 'selectSubject' FRAME-Redesign/beta/app/src/main/assets/app.js
          grep -q 'FrameAI' FRAME-Redesign/beta/app/src/main/java/com/ilia/frame/redesignbeta/MainActivity.java
          grep -q 'subject-segmentation' FRAME-Redesign/beta/app/build.gradle
          grep -q "versionName '0.4.0-beta4'" FRAME-Redesign/beta/app/build.gradle

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.10.2
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 35
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Build FRAME Beta
        run: gradle --no-daemon -p FRAME-Redesign/beta :app:assembleDebug

      - name: Verify APK identity
        shell: bash
        run: |
          APK="FRAME-Redesign/beta/app/build/outputs/apk/debug/app-debug.apk"
          test -f "$APK"
          "$ANDROID_HOME/build-tools/35.0.0/aapt" dump badging "$APK" | tee FRAME-Redesign/beta/apk-badging.txt
          grep -q "package: name='com.ilia.frame.redesignbeta.debug'" FRAME-Redesign/beta/apk-badging.txt
          grep -q "versionName='0.4.0-beta4-debug'" FRAME-Redesign/beta/apk-badging.txt

      - name: Collect beta artifact
        shell: bash
        run: |
          mkdir -p frame-beta-apk
          cp FRAME-Redesign/beta/app/build/outputs/apk/debug/app-debug.apk frame-beta-apk/FRAME-Redesign-Beta-0.4.0.apk
          cp FRAME-Redesign/beta/apk-badging.txt frame-beta-apk/APK-IDENTITY.txt
          sha256sum frame-beta-apk/FRAME-Redesign-Beta-0.4.0.apk | tee frame-beta-apk/SHA256SUMS.txt
          ls -lh frame-beta-apk

      - name: Upload FRAME beta APK
        uses: actions/upload-artifact@v4
        with:
          name: FRAME-REDESIGN-BETA-0.4.0
          path: frame-beta-apk/*
          if-no-files-found: error
          retention-days: 30
